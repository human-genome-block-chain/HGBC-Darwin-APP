// MainTab部分
const normalHome = require('./normalHome.webp')
const selectedHome = require('./selectedHome.webp')
const normalMine = require('./normalMine.webp')
const selectedMine = require('./selectedMine.webp')
const normalApplication = require('./application.webp')
const selectedApplication = require('./selectedApplication.webp')

export {
	normalHome,
	selectedHome,
	normalMine,
	selectedMine,
	normalApplication,
	selectedApplication
}
